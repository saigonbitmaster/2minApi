{
  "name": "2min",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "api",
    "packages/*"
  ],
  "main": "index.js",
  "scripts": {
    "api": "cd api && yarn start",
    "build-api": "cd api && yarn build"
  },
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^5.2.0"
  }
}
